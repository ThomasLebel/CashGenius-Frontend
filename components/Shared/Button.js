
function Button({name}) {
  return (
    <div>
        <button className=' bg-[#ce0251] hover:bg-[#ac054a] text-white font-sans font-semibold text-xs p-4 rounded-md border border-white'>{name}</button>
    </div>
  );
}

export default Button;
